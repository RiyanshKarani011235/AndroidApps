<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ~~~~~~~~~ book_card_layout.xml styles ~~~~~~~~~~~~~ -->

    <style name="BookCardLayout_background">
        <item name="android:layout_height">200dp</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:elevation">5dp</item>
    </style>

    <style name="BookCardLayout_image">
        <item name="android:layout_margin">8dp</item>
        <item name="android:maxWidth">100dp</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:maxHeight">100dp</item>
    </style>

    <style name="BookCardLayout_title">
        <item name="android:textSize">20sp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginBottom">0dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="BookCardLayout_authors_tab">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="BookCardLayout_authors">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="BookCardLayout_link_buttons">
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <!-- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -->

</resources>
